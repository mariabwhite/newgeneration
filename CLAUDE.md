# Project Rules — ЭТАЛОН (UNIFIED DESIGN)

Этот файл — контракт для любого AI-агента, работающего в этой папке (Claude Code, Codex CLI, и др.). Читать ПЕРЕД любым действием с файлами.

---

## 1. Scope

- Работаешь ТОЛЬКО внутри этой папки: `САМАЯ УДАЧНАЯ КОПИЯ - ЭТАЛОН!`
- Не лезь в `..\` (соседние папки проекта) и не смешивай состояния разных копий.
- В GitHub НЕ пушишь без явной команды пользователя.
- Локальный git — твой друг. Коммить часто, после каждого осмысленного блока.

## 2. Параллельные агенты — стоп-сигнал

Если видишь свежие правки в файлах, которые не делал ты сам — это другой агент (Codex CLI, IDE-плагин, другой Claude). СТОП. Спроси пользователя.

Не пиши параллельно: 14 мая 2026 этим способом потеряли 24 урока Lab — Codex обрезал хвосты файлов одновременно с моими правками. Повторять не надо.

Признаки активного второго агента:
- В .git/ внезапно появляются `index.lock`, `HEAD.lock` и они не пропадают
- Только что прочитанный файл при перечитывании отличается
- В файлах появляются маркеры с временной меткой (типа `codex-a1-body-calm-20260514`)

## 3. Перед каждой записью

- Прочитай файл целиком (не фрагмент).
- Зафиксируй: размер в байтах, md5, последние 50 байт.
- Для HTML: проверь, что заканчивается на `</html>`.
- Для файлов с кириллицей: открой как UTF-8, проверь стабильность байтовой длины.

## 4. После каждой записи

- Перечитай файл.
- Сверь размер с ожидаемым (если дописал 100 байт — должно быть +100, не -500).
- Для HTML: проверь, что всё ещё заканчивается на `</html>`.
- Для CSS: если правка видимая — bump `?v=N` во всех HTML, которые его подключают. Иначе браузер увидит закэшированный старый файл и пользователь решит, что всё сломалось.
- После CSS-правок — проверь обе темы (dark + light).

## 5. Read-only зоны (НЕ трогать без явного разрешения)

- `_archive/` — версионные снапшоты, исторический архив
- `_internal/`, `_backups/`, любые `*.bak`, `*.broken`, `*.nullbackup-*`
- `audit-shots/` — Chrome profile dumps от Codex, не редактировать
- `cabinet/*.html` — кабинеты учеников/родителей/учителей
- `lingua-boost-lab/a1/`, `a2/`, `b1/`, `c1/`, `pre-a1/` — уроки Lab (только по явному запросу на конкретный урок)
- `assets/designs of lessons for kids/`, `assets/Для уроков/` — черновики DeepSeek

## 6. CSS refactor (домен J · унификация)

Самая чувствительная зона. Правила:

- Одна страница за раз. НИКОГДА пачкой.
- Снапшот до и после (минимум: размер + md5; идеально: скриншот в браузере).
- Старые CSS-классы оставлять как aliases в `legacy.css`, если на них завязан JS.
- Архитектура слоёв:
  - `system.css` — core, базовые токены и примитивы
  - `<page>.css` или inline `<style>` — только уникальные блоки страницы
  - `canon-L.css` — Lab-specific (не смешивать с system.css)
  - `legacy.css` — временные алиасы для старых классов
- Локальные `:root` на страницах должны исчезнуть — токены живут только в `system.css`.
- Тест в обеих темах после каждого шага.

## 7. Восстановление при поломке

Если файл оказался обрезан/побит:

1. Проверь git: `git log -- <файл>`, `git show HEAD:<файл>`. Самое надёжное.
2. Если в git нет — поищи в `_archive/`, `_backups/`, `_internal/backups/`. Будь осторожна: «свежий» бэкап может уже быть битым (история обрезок).
3. Если в бэкапах тоже битые — спроси пользователя. Не реконструируй молча.

## 8. Cyrillic / Edit tool

Edit tool иногда обрезает хвост файлов с кириллицей при кажущемся успехе. Поэтому:

- На больших файлах с кириллицей предпочитай Python через bash вместо Edit.
- ВСЕГДА после Edit/Write: `wc -c`, `tail -c 50`, `md5` — и сравни с ожидаемым.
- Если файл «съехал» — немедленно откат из git, не накатывай следующую правку.

## 9. Сомневаешься — стоп

- Не угадывай. Спроси пользователя.
- Не «доделывай интуитивно» — лучше один уточняющий вопрос, чем час откатов.
- Перед массовой операцией (>3 файлов) — покажи пользователю что собираешься делать, дождись «да».

---

## Контекст проекта (для AI: чтобы понимать, о чём речь)

**Что это:** сайт NG English / LinguaBoost Lab — школа английского + лаборатория интерактивных уроков. Преподаватель: Maria.

**8 главных страниц:** `index.html`, `about-project.html`, `programs.html`, `cases.html`, `conditions.html`, `blog.html`, `travel.html`, `diagnostic-test.html`.

**Кабинеты:** `cabinet/index.html`, `login.html`, `parent.html`, `student.html`, `teacher.html`.

**Lab:** `lingua-boost-lab/index.html` (каталог) + `find-and-say-teaser.html` + уроки в подпапках `a1/`, `a2/`, `b1/`, `c1/`, `pre-a1/`.

**Дизайн-токены:** оранжевый акцент `#FF5A1F` (main) и `#FFC145` (Lab accent); тёмная тема — default, светлая — переключатель.

**JS:** `site.js` для основного сайта (язык, тема), `canon-L.js` для Lab, `*-standalone.js` для кабинетов.

---

## Механика: Claude ↔ Codex ↔ Maria (добавлено 2026-05-24)

Реальный рабочий процесс по сайту — кто что делает и через что.

### Распределение ролей
- **Claude (эта сессия)** — править файлы локально, читать/диагностировать, обновлять `SITE-LOG.md` и `Шорт лист`. **НЕ пушит в GitHub.**
- **Codex** (в Telegram через AI Hub) — пушит локальные коммиты на GitHub Pages master, может делать свои правки по конкретному запросу. Триггер `@codex!` (терминальный режим, может запускать команды).
- **Maria** — принимает решения по дизайну/контенту, проверяет результат на телефоне (`mariabwhite.github.io`), даёт обратную связь.

### AI Hub (мост Claude ↔ Telegram)
- Локальный HTTP-сервер: `http://127.0.0.1:8765`
- Chat ID Маши: `335307188`
- Папка моста: `08_Projects\03_AI Hub — Telegram чат, агенты, диктовка\AI_Chat_Hub\`
- Картинки из Telegram сохраняются в `AI_Chat_Hub\telegram_media\` — я их читаю как multimodal через Read tool.

**Отправить промт в Telegram-чат Маши/Codex:**
```powershell
$body = @{ chatId = "335307188"; text = "@codex! ..." } | ConvertTo-Json
Invoke-RestMethod -Uri "http://127.0.0.1:8765/api/send-telegram" -Method Post -ContentType "application/json; charset=utf-8" -Body $body
```

**Прочитать всю историю чата:**
```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:8765/api/messages" -Method Get
```

### Стандартный цикл правок «волной»
1. **Backup-tag** перед волной: `git tag backup-YYYY-MM-DD-pre-<scope>`.
2. **Правки** в коде (CSS/JS/HTML).
3. **Bump version** в HTML-ссылках на CSS/JS — иначе браузеры берут закэшированный старый файл. Делать **только byte-level replace** (см. ниже).
4. **Verify** — после каждого bump проверить что HTML начинается с `3C 21 44` (`<!D`) и заканчивается на `</html>`.
5. **Обновить `SITE-LOG.md`** новой записью + при необходимости `Шорт лист`.
6. **Локальный `git commit`** с понятным сообщением + Co-Authored-By Claude.
7. **Промт `@codex!` в Telegram** через AI Hub — он запушит на GitHub Pages.
8. **Проверка Маши** на телефоне (`mariabwhite.github.io`), скрины через Telegram → я читаю и реагирую.

### Bump версий — byte-level через PowerShell
**Не использовать UTF8.GetString → -replace → UTF8.GetBytes** — этот путь однажды съел первый байт `<` во всех 13 HTML (волна 1 mobile K-1, 2026-05-22). Корень не понят до конца, но симптом: BOM появлялся вместо `<`.

**Правильно — byte-level pattern replace:**
```powershell
function Replace-BytesInFile {
  param([string]$Path, [string]$Search, [string]$Replace)
  $b = [System.IO.File]::ReadAllBytes($Path)
  $s = [System.Text.Encoding]::UTF8.GetBytes($Search)
  $r = [System.Text.Encoding]::UTF8.GetBytes($Replace)
  $out = New-Object System.Collections.Generic.List[byte]
  $i = 0
  while ($i -lt $b.Length) {
    $match = ($i + $s.Length -le $b.Length)
    if ($match) { for ($j = 0; $j -lt $s.Length; $j++) { if ($b[$i+$j] -ne $s[$j]) { $match = $false; break } } }
    if ($match) { foreach ($byte in $r) { $out.Add($byte) }; $i += $s.Length }
    else { $out.Add($b[$i]); $i++ }
  }
  [System.IO.File]::WriteAllBytes($Path, $out.ToArray())
}
```
ASCII-safe, кириллицу не трогает, BOM сохраняет/не добавляет.

### Backup-теги (откатные точки)
Все теги — в `git tag | grep backup-`. Откат: `git reset --hard <tag>`.

Сегодняшние:
- `backup-2026-05-21-pre-seo-foundation`
- `backup-2026-05-22-pre-diagnostic-inline-extract`
- `backup-2026-05-22-pre-k17-blog`
- `backup-2026-05-22-pre-mobile-K1`

### Шорт-лист / Резюме сессии — НЕ в git
В `.gitignore`: `Шорт лист - актуальные дела.md`, `Резюме сессии*.md`, `Listening-скрипты*.md`. Это локальные рабочие документы Маши. SITE-LOG — единственный canonical-журнал в git.

### Источник правды по статусам
- **Что сделано** → `SITE-LOG.md` (по дням)
- **Что осталось делать** → `Шорт лист - актуальные дела.md` (М-* для Маши, К-* для Claude, C-* для Codex)
- **Правила работы AI** → этот файл

---

*Последняя правка: 2026-05-24 — добавлен раздел «Механика». При обновлении правил — добавляй дату.*
